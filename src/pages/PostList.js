import React from 'react';
import Grid from "../elements/Grid";
const PostList = (props) => {
    return (
        <React.Fragment>
            <Grid>
                
            </Grid>
        </React.Fragment>
    );
};

export default PostList;