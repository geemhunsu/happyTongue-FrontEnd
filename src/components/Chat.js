import React from 'react';
import { Grid, Button, Image, Text, Input } from '../elements';

const Chat = (props) => {
    return (
        <React.Fragment>
            <Grid>
                
            </Grid>
        </React.Fragment>
    );
};


export default Chat;